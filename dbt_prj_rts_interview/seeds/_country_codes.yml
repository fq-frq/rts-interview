version: 2
seeds:
  - name: country_codes
    config:
      quote_columns: false
      column_types: {name: string, code: string}
      delimiter: ','
    columns:
      - name: name
        data_type: STRING
      - name: code
        data_type: STRING
